JSONComparator is using maven as build management tool with JAVA as binding language. It has capability of running in multiple threads which is implemented via TestNG.
It take two files as inputs which has capability to support millions of end points .
The files has to be pasted under resources -> test data. This comparator will compare the two JSON response and returns the results accordingly.
